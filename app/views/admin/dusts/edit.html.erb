<h1>投稿編集</h1>
<% if @dust.errors.any? %>
  <%= @dust.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @dust.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: @dust, url: admin_dust_path(@dust), method: :patch do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit '再投稿' %>

<% end %>

<%= link_to "詳細", admin_dust_path(@dust.id) %>
<%= link_to "戻る", admin_dusts_path %>