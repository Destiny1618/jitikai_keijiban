<% @dusts.each do |dust| %>
  <div>
    <%= link_to dust_path(dust.id) do %>
      <%= dust.title %>
    <% end %>
    <p>投稿者：<%= dust.customer.nil? ? dust.admin.name : dust.customer.nickname %></p>
  </div>
<% end %>