<% @posts.each do |post| %>
  <div>
    <%= link_to post_path(post.id) do %>
      <%= post.title %>
    <% end %>
    <p>投稿者：<%= post.customer.nil? ? post.admin.name : post.customer.nickname %></p>
    <p><%= link_to "#{post.post_comments.count} コメント", post_path(post.id) %></p>
  </div>
<% end %>